diff --git a/examples/llvm_mir_p4/default.mir b/examples/llvm_mir_p4/default.mir
new file mode 100644
index 0000000..24e92f5
--- /dev/null
+++ b/examples/llvm_mir_p4/default.mir
@@ -0,0 +1,13 @@
+---
+name:            square
+tracksRegLiveness: true
+body:             |
+  bb.0:
+    liveins: $edi
+
+    %0:gr32 = COPY $edi
+    %1:gr32 = nsw IMUL32rr %0, %0, implicit-def dead $eflags
+    $eax = COPY %1
+    RET 0, $eax
+
+...
diff --git a/lib/languages.ts b/lib/languages.ts
index e33f185..d5b6608 100644
--- a/lib/languages.ts
+++ b/lib/languages.ts
@@ -685,6 +685,17 @@ const definitions: Record<LanguageKey, LanguageDefinition> = {
         previewFilter: null,
         monacoDisassembly: null,
     },
+    llvm_mir_p4: {
+        name: 'LLVM MIR P4',
+        monaco: 'llvm-ir',
+        extensions: ['.mir'],
+        alias: [],
+        logoFilename: 'p4.svg',
+        logoFilenameDark: null,
+        formatter: null,
+        previewFilter: null,
+        monacoDisassembly: null,
+    },
     llvm_p4: {
         name: 'LLVM IR P4',
         monaco: 'llvm-ir',
diff --git a/types/languages.interfaces.ts b/types/languages.interfaces.ts
index 96d708d..581c1e8 100644
--- a/types/languages.interfaces.ts
+++ b/types/languages.interfaces.ts
@@ -78,6 +78,7 @@ export type LanguageKey =
     | 'lean'
     | 'llvm'
     | 'llvm_mir'
+    | 'llvm_mir_p4'
     | 'llvm_p4'
     | 'lua'
     | 'makefile'
