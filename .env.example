# 宿主机工具链根目录（绝对路径）—— 改成你实际放置 Clang/GCC/MLIR 的位置。
# 该目录会以只读方式挂进 CE 容器的 /opt/compiler-explorer。
# 使用方法：cp .env.example .env 然后改成实际路径（.env 不进 git）。
CE_COMPILERS_ROOT=/srv/ce/compilers

# ---- 可选：供应链完整性钉值（推荐在会被漏洞探测的环境使用）----
# 把下面三个填成官方/核对过的 SHA256，update-clang-gcc.sh 每次下载都会强制比对，
# 不符即中止、不解压。留空则只打印实际哈希供你核对。
# 首次获取：先留空跑一次，记下脚本打印的 SHA256，再填回来。
#LLVM_SHA256=
#GCC_SHA256=
#GCC_RISCV_SHA256=
