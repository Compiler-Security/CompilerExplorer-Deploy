# 执行沙箱开关。
# 隔离由 Kata VM（hypervisor 边界）提供，不再用 nsjail —— 两个都 none。
# 编译器/工具直接在容器（VM 内）里以非 root 运行；「在线运行」是否开放由语言的
# supportsExecute 控制（当前 c++.local.properties 里为 false，即只编译不运行）。
# 因为运行在 VM 里，即便日后把 supportsExecute 改 true，用户程序也只会跑在 VM 内。

sandboxType=none
executionType=none
